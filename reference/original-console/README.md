# Original Console Reference

Downloaded from:

```text
http://116.172.93.164:28080/
```

This folder contains the original static frontend bundle for implementation reference:

- `index.html`
- `favicon.ico`
- `static/css/*`
- `static/js/*`
- CSS-referenced fonts under `static/fonts/*`

Notes:

- This is a static snapshot of the original web console frontend only.
- It does not include backend data, login credentials, cookies, or API responses.
- The app bundle is minified, but it is useful for checking routes, request payloads, labels, status mappings, and UI behavior.
- The original frontend uses `/api` as its API base and routes such as `/example-statistic/list` for instance statistics.
